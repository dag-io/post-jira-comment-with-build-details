format_version: 1.1.0
default_step_lib_source: https://github.com/bitrise-io/bitrise-steplib.git

workflows:
  test:
    steps:
    - path::./:
        title: Foo bar
        description: |-
          Foo bar
        inputs:
        - git_branch: "feature/IOS-123-Add-FooBar"
        - jira_user: "foo"
        - jira_password: "bar"
        - jira_domain: "foobar.lol.com"
        - jira_build_message: |-
            Hello There
            What's up ?
